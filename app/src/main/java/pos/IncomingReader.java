package pos;

public class IncomingReader implements Runnable{
    public void run (){
        String inputStr;
//        setUpNetworking();
        try {
////                TimeUnit.SECONDS.sleep(10);
//            System.out.println("Connected for receiving...");
//            while ((inputStr = reader.readLine()) != null){
//                if (isHasHash(inputStr)) {
////                            &  (checkDto.hashCode() == extractHash(inputStr))) {
//                    textCheck.setText("");
//                    checkDto.clear();
//
//                    saleBtn.setEnabled(true);
//                    cancelBtn.setEnabled(true);
//                }
//            }
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}